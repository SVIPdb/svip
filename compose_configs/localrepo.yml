version: '3.5'

# puts all images for the project into a local repository

services:
  nginx:
    image: localhost:15000/svip/nginx:latest
  api:
    image: localhost:15000/svip/api:latest
  frontend:
    image: localhost:15000/svip/frontend:latest
  db:
    image: localhost:15000/svip/db:latest
  redis:
    image: localhost:15000/svip/redis:latest
